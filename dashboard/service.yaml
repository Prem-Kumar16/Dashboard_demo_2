apiVersion: v1
kind: Service
metadata:
  name: dashboarddemo-app-service
spec:
  ports:
     - name: http
       protocol: TCP
       port: 4000
       targetPort: 4000
  selector:
    app: dashboarddemo
